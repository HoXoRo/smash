# 箭头迷宫解谜游戏 - 核心玩法逻辑分析

## 一、核心玩法概述

### 1.1 游戏目标
- **胜利条件**：让所有箭头线成功移出屏幕外
- **失败条件**：箭头碰撞到其他箭头时扣除生命，生命归零则失败
- **生命系统**：初始3条生命，每次碰撞扣除1条

### 1.2 基本操作
- **点击箭头线**：箭头会沿着其当前方向向前移动一步
- **移动方向**：由箭头的最后两个节点决定（只能沿四个基本方向：上、右、下、左）
- **连续移动**：如果前方无障碍，可以连续移动（`autoRunToEnd`）

---

## 二、核心系统架构

### 2.1 网格系统 (GridManager) - **基础层**

**职责**：
- 管理游戏网格的创建和维护
- 提供网格坐标与世界坐标的双向转换
- 管理Dot节点的生成和查找

**核心数据结构**：
```csharp
- width, height: 网格尺寸（整数）
- spacing: 节点间距（浮点数）
- origin: 网格原点（Vector3）
- dots[,]: 二维数组存储所有Dot节点
- dotByGrid: Dictionary<Vector2Int, Dot> 快速查找
```

**核心方法**：
- `RebuildSmart()`: 智能重建网格（复用已有节点，优化性能）
- `GridToWorld(Vector2Int)`: 网格坐标 → 世界坐标
- `WorldToGrid(Vector3)`: 世界坐标 → 网格坐标
- `GetDotByGrid(Vector2Int)`: 根据网格坐标获取Dot节点

**设计要点**：
- 使用二维数组 + Dictionary双重索引，兼顾性能和查找便利性
- 网格坐标使用`Vector2Int`确保精确性（整数坐标）

---

### 2.2 节点系统 (Dot) - **基础层**

**职责**：
- 表示网格上的一个点
- **核心功能**：跟踪占用状态（哪个箭头线占用此节点）

**关键属性**：
```csharp
- G: Vector2Int 网格坐标
- occupant: ArrowLine 占用此节点的箭头线（null表示空闲）
- IsFree: bool 是否空闲（occupant == null）
```

**方法**：
- `HideDot()` / `ShowDot()`: 控制节点视觉显示（可选）

**设计要点**：
- **占用机制是碰撞检测的核心**：通过检查`Dot.occupant`判断是否发生碰撞
- 每个Dot节点只能被一个ArrowLine占用

---

### 2.3 箭头线系统 (ArrowLine) - **核心层**

**职责**：
- 控制箭头线的移动、碰撞检测、退出动画
- 处理用户输入（点击检测）
- 管理LineRenderer和EdgeCollider2D的同步

**核心数据结构**：
```csharp
// 路径数据
public List<Dot> nodes = new();        // 箭头经过的所有节点（有序列表）
private int startIndex = 0;             // 当前头部在nodes中的索引

// 视觉组件
private LineRenderer lr;               // 线条渲染器
private EdgeCollider2D edge;           // 边缘碰撞器（用于点击检测）
public Transform headVisual;           // 头部视觉对象

// 运动状态
private bool isMoving = false;         // 是否正在移动
private int queuedSteps = 0;            // 排队的移动步数（最多3步）
private bool isExiting = false;        // 是否正在退出屏幕
private Vector3 exitStep;              // 退出时的每步移动量

// 配置参数
public float stepTime = 0.18f;          // 每步移动时间
public bool occupyAllNodes = true;     // 是否占用所有节点
public bool autoRunToEnd = true;       // 是否自动运行到终点
```

**核心方法流程**：

#### 1. **TryAdvance()** - 尝试向前移动
```csharp
public void TryAdvance()
{
    if (isExiting) return;  // 正在退出，忽略输入
    
    if (isMoving) 
    { 
        // 正在移动中，将步骤加入队列（最多3步）
        queuedSteps = Mathf.Min(queuedSteps + 1, 3); 
        return; 
    }
    
    StepOnce();  // 立即执行移动
}
```

**设计要点**：
- 队列机制：移动中可以排队最多3步，防止过度点击
- 退出模式下忽略所有输入

#### 2. **StepOnce()** - 执行一步移动（核心算法）

**流程步骤**：

**步骤1：计算移动方向**
```csharp
int n = nodes.Count;
if (n < 2) return;

var head = nodes[n - 1];      // 当前头部节点
var prev = nodes[n - 2];      // 前一个节点

// 方向 = 头部 - 前一个节点（网格坐标差）
Vector2Int dir = head.G - prev.G;
Vector2Int nextG = head.G + dir;  // 下一个目标网格坐标
```

**步骤2：查找目标节点**
```csharp
Dot nextDot = FindDotByGrid(nextG);
if (nextDot == null)
{
    // 没有找到节点 = 到达网格边界
    Vector3 stepWorld = nodes[n - 1].transform.position - nodes[n - 2].transform.position;
    onLineExitedAction?.Invoke();
    StartExit(stepWorld);  // 进入退出模式
    return;
}
```

**步骤3：碰撞检测** ⚠️ **核心机制**
```csharp
// 占用检查（忽略自身）
if (nextDot.occupant != null && nextDot.occupant != this)
{
    // 发生碰撞！
    onLineHitAction?.Invoke();  // 触发碰撞事件
    
    // 红色闪烁效果
    SetLineColor(hitColor);
    Invoke(nameof(ResetLineColor), 0.08f);
    
    queuedSteps = 0;  // 清空队列
    return;  // 停止移动
}
```

**碰撞检测原理**：
- 通过`Dot.occupant`检查目标节点是否被占用
- 忽略自身占用（`occupant != this`）
- 碰撞时触发事件，扣除生命，显示红色闪烁

**步骤4：更新路径（移动成功）**
```csharp
// 使用"滑动窗口"机制：移除尾部，添加新头部
Dot tail = nodes[0];
if (occupyAllNodes && tail && tail.occupant == this) 
    tail.occupant = null;  // 释放尾部节点

// 所有节点向前移动一位
for (int i = 0; i < n - 1; i++) 
    nodes[i] = nodes[i + 1];
nodes[n - 1] = nextDot;  // 新头部

// 占用新头部节点
SafeClaim(nextDot);
```

**路径更新逻辑**：
- 保持箭头长度不变（nodes.Count）
- 释放尾部节点占用，占用新头部节点
- 使用"滑动窗口"机制实现移动

**步骤5：动画移动**
```csharp
isMoving = true;

// 使用线性插值平滑移动
// 每帧更新LineRenderer的所有点位置
// 同步更新EdgeCollider2D（用于点击检测）
// 更新头部视觉位置和旋转

// 移动完成后处理队列或自动继续
if (queuedSteps > 0 || autoRunToEnd)
{
    queuedSteps--;
    StepOnce();  // 递归调用，继续移动
}
```

#### 3. **点击检测** (Update方法)
```csharp
void Update()
{
    // 检测点击/触摸
    bool down = Input.GetMouseButtonDown(0)
                || (Input.touchCount > 0 && Input.GetTouch(0).phase == TouchPhase.Began);
    if (!down) return;
    
    // 屏幕坐标转世界坐标
    Vector3 w = camUse.ScreenToWorldPoint(sp);
    
    // 使用圆形碰撞检测（半径基于线条宽度）
    float r = Mathf.Max(lr.startWidth, lr.endWidth) * 0.6f;
    var hits = Physics2D.OverlapCircleAll(p2, r);
    
    // 检查是否点击到当前箭头
    foreach (var h in hits)
    {
        if (h.transform == transform)
        {
            TryAdvance();
            break;
        }
    }
}
```

**设计要点**：
- 使用`EdgeCollider2D`进行点击检测，碰撞器随线条动态更新
- 检测半径基于LineRenderer宽度，确保点击区域合理
- 支持鼠标和触摸输入

#### 4. **退出机制** (StartExit / ExitStepOnce)
```csharp
void StartExit(Vector3 stepWorld)
{
    isExiting = true;
    ReleaseAllNodes();      // 释放所有占用的节点
    exitStep = stepWorld;   // 保存每步移动量
    ExitStepOnce();         // 开始退出循环
}

void ExitStepOnce()
{
    // 使用"滑动窗口"机制：身体向前移动，头部持续延伸
    // 每步移动后检查是否完全离开屏幕
    if (IsCompletelyOffscreen())
    {
        LevelManager.Instance?.OnLineExited(this);
        Destroy(gameObject);  // 销毁箭头
    }
    else
    {
        ExitStepOnce();  // 继续退出
    }
}
```

**退出机制特点**：
- 到达网格边界后自动进入退出模式
- 释放所有节点占用，允许其他箭头使用这些节点
- 持续向前移动直到完全离开屏幕

---

### 2.4 关卡系统 (LevelManager) - **管理层**

**职责**：
- 管理关卡加载和切换
- 跟踪所有生成的箭头线
- 处理胜利/失败逻辑

**关键属性**：
```csharp
- levels: List<LevelAsset> 所有关卡资源
- currentLevelIndex: int 当前关卡索引
- levelState: LevelState 关卡状态（NotStarted/Playing/Completed/Failed）
- startLives: int 初始生命数（3）
- _spawnedLines: List<ArrowLine> 所有生成的箭头线
```

**核心方法**：

1. **LoadLevel(int index)** - 加载关卡
   - 清除所有现有箭头
   - 从LevelAsset加载网格配置
   - 根据ArrowLineDef列表生成所有箭头线
   - 初始化Dot索引

2. **TrySpawnLineFromDef(ArrowLineDef)** - 从定义生成箭头线
   - 将Vector2Int路径转换为Dot列表
   - 实例化ArrowLine预制体
   - 配置颜色、速度等属性
   - 调用SyncVisualImmediate()同步视觉

3. **OnLineExited(ArrowLine)** - 箭头线成功退出
   - 从_spawnedLines移除
   - **如果所有箭头都退出，触发胜利事件**

4. **OnLineHit()** - 箭头碰撞
   - 通知UiManager扣除生命
   - 播放碰撞音效

---

### 2.5 关卡资源系统 (LevelAsset) - **数据层**

**ScriptableObject结构**：
```csharp
LevelAsset
├── levelName: string
├── grid: GridDef (width, height, spacing, origin)
├── arrows: List<ArrowLineDef>
├── camSize: float
├── coinsEarned: int
└── levelDifficulty: LevelDifficulty

ArrowLineDef
├── path: List<Vector2Int>  // 箭头路径（网格坐标）
├── stepTime: float
├── lineColor: Color
├── hitColor: Color
├── occupyAllNodes: bool
└── startIndex: int
```

**设计要点**：
- 使用ScriptableObject实现数据驱动
- 路径使用网格坐标（Vector2Int）存储，易于编辑和序列化

---

## 三、关键机制详解

### 3.1 碰撞检测机制 ⚠️ **核心机制**

**原理**：
- 每个Dot节点有一个`occupant`属性，指向占用此节点的ArrowLine
- 当箭头移动时，检查目标节点的`occupant`
- 如果`occupant != null && occupant != this`，则发生碰撞

**实现位置**：`ArrowLine.StepOnce()`方法中

**碰撞处理**：
1. 触发`onLineHitAction`事件
2. 显示红色闪烁效果（0.08秒）
3. 清空移动队列
4. 停止移动

**设计优势**：
- 简单高效：O(1)时间复杂度
- 精确可靠：基于网格坐标，避免浮点数误差
- 易于调试：可以直观查看每个节点的占用状态

---

### 3.2 路径移动机制

**"滑动窗口"机制**：
- 箭头长度固定（nodes.Count）
- 移动时：移除尾部节点，添加新头部节点
- 保持箭头长度不变，实现平滑移动

**节点占用管理**：
- 移动前：释放尾部节点占用
- 移动后：占用新头部节点
- `occupyAllNodes = true`时，箭头占用路径上的所有节点

---

### 3.3 退出机制

**触发条件**：
- 当箭头移动到网格边界外时，`FindDotByGrid()`返回null

**退出流程**：
1. 调用`StartExit()`进入退出模式
2. 释放所有占用的节点（允许其他箭头使用）
3. 持续向前移动（`ExitStepOnce()`）
4. 检查是否完全离开屏幕（`IsCompletelyOffscreen()`）
5. 如果离开，销毁箭头并通知LevelManager

**屏幕外检测**：
- 考虑Z轴偏移，正确计算屏幕边界
- 使用边距（exitMargin）避免边界闪烁
- 只有所有点都在屏幕外才认为完全离开

---

### 3.4 视觉同步系统

**三个组件需要同步**：
1. **LineRenderer**：绘制线条视觉
2. **EdgeCollider2D**：用于点击检测（使用本地坐标）
3. **头部视觉**：箭头头部位置和旋转

**同步方法**：
- `SyncVisualImmediate()`: 立即同步LineRenderer位置
- `SyncEdgeFromLR()`: 同步EdgeCollider2D（世界坐标→本地坐标）
- `UpdateHeadFromLR()`: 更新箭头头部视觉位置和旋转

**头部旋转计算**：
- 根据最后两个节点的方向计算角度
- 使用`DirToZ()`将方向转换为Z轴旋转角度
- 支持四个基本方向：上(0°)、右(-90°)、下(180°)、左(90°)

---

### 3.5 状态管理

**箭头状态机**：
```
[Idle] 静止状态，等待输入
  │
  │ TryAdvance()
  ▼
[Moving] 正在移动中
  │
  │ (移动完成)
  ▼
[Idle] 或 [Moving] (如果autoRunToEnd或queuedSteps > 0)
  │
  │ (到达边界)
  ▼
[Exiting] 退出屏幕中
  │
  │ (持续移动)
  ▼
[Destroyed] 已销毁
```

**关键标志位**：
- `isMoving`: 防止移动过程中的重复输入
- `isExiting`: 退出模式下忽略所有输入
- `queuedSteps`: 队列机制，最多3步
- `autoRunToEnd`: 自动连续移动标志

---

## 四、游戏流程

### 4.1 关卡加载流程
```
1. LevelManager.Start()
   └─> LoadLevel(currentLevelIndex)
       ├─> ClearAllArrows() 清除旧箭头
       ├─> GridManager.ApplyFromAsset() 应用网格配置
       ├─> BuildDotIndex() 构建Dot索引
       └─> TrySpawnLineFromDef() 为每个ArrowLineDef生成箭头
```

### 4.2 游戏进行流程
```
1. 玩家点击箭头线
   └─> ArrowLine.Update() 检测点击
       └─> TryAdvance()
           └─> StepOnce()
               ├─> 计算下一个节点
               ├─> 检查碰撞
               │   ├─> 碰撞：触发OnLineHit() → 扣除生命
               │   └─> 无碰撞：移动箭头
               └─> 检查是否到达边界
                   └─> 到达：StartExit() → 退出动画
```

### 4.3 胜利/失败流程

**胜利**：
```
所有箭头退出
└─> OnLineExited() 被调用
    └─> _spawnedLines.Count == 0
        └─> onLevelWin.Invoke()
            └─> UiManager.LevelComplete()
```

**失败**：
```
生命归零
└─> UiManager.RemoveLife() → lives = 0
    └─> Invoke("LevelFailed", 2f)
        └─> 显示失败UI
```

---

## 五、技术要点总结

### 5.1 数据结构
- **网格系统**：二维数组 + Dictionary双重索引
- **路径存储**：List<Dot> 有序节点列表
- **关卡数据**：ScriptableObject序列化

### 5.2 坐标系统
- **网格坐标（Vector2Int）**: 用于逻辑计算，确保精确性
- **世界坐标（Vector3）**: 用于渲染
- **本地坐标（Vector2）**: 用于碰撞器

### 5.3 动画系统
- 使用DOTween（可选）进行平滑移动
- 线性插值更新LineRenderer位置
- 支持无DOTween的回退方案

### 5.4 输入处理
- 支持鼠标和触摸
- 使用Physics2D.OverlapCircleAll进行点击检测
- 检测半径基于LineRenderer宽度

### 5.5 事件驱动
- 使用UnityEvent进行解耦
- `onLineExitedAction`: 箭头退出事件
- `onLineHitAction`: 箭头碰撞事件

---

## 六、核心玩法逻辑总结

### 6.1 最核心的三个机制

1. **节点占用机制**（Dot.occupant）
   - 这是碰撞检测的基础
   - 每个节点只能被一个箭头占用
   - 通过检查目标节点的occupant判断是否碰撞

2. **路径滑动窗口机制**（List<Dot>动态更新）
   - 箭头长度固定
   - 移动时移除尾部，添加新头部
   - 保持箭头长度不变

3. **方向计算机制**（最后两个节点决定方向）
   - 箭头方向由最后两个节点决定
   - 使用网格坐标（Vector2Int）确保方向精确
   - 只支持四个基本方向

### 6.2 游戏核心乐趣

**规划点击顺序**：
- 玩家需要规划每个箭头的点击顺序
- 避免箭头相互碰撞
- 在3次失误内完成所有箭头的解谜

**策略性**：
- 需要预判箭头移动后的位置
- 考虑多个箭头的移动顺序
- 平衡风险和效率

---

## 七、实现优先级建议

### 7.1 核心系统优先级
1. **GridManager** - 网格系统（基础，必须最先实现）
2. **Dot** - 节点系统（基础，与GridManager同时实现）
3. **ArrowLine** - 箭头线系统（核心，最复杂）
4. **LevelManager** - 关卡管理（核心，依赖前三个）
5. **LevelAsset** - 关卡资源（数据，可并行开发）

### 7.2 实现顺序
1. ✅ 创建网格系统和Dot节点
2. ✅ 实现ArrowLine基本移动（不含碰撞）
3. ✅ 添加碰撞检测
4. ✅ 实现退出机制
5. ✅ 添加关卡加载系统
6. ✅ 实现UI和生命系统
7. ⚠️ 添加编辑器工具（可选，用于关卡编辑）

---

## 八、与UnityGameFramework集成要点

### 8.1 需要适配的框架模块

1. **Procedure系统**：关卡加载流程
2. **UI系统**：游戏UI、胜利/失败UI
3. **Entity系统**：箭头线可以作为Entity管理（可选）
4. **DataTable系统**：关卡数据可以存储在DataTable中（可选）
5. **Event系统**：碰撞、退出等事件可以使用框架事件系统
6. **Sound系统**：碰撞音效、背景音乐

### 8.2 建议的集成方式

- **GridManager**: 可以作为单例Manager，在Procedure中初始化
- **LevelManager**: 可以作为单例Manager，管理关卡流程
- **ArrowLine**: 可以作为Entity或普通MonoBehaviour
- **LevelAsset**: 可以作为ScriptableObject或DataTable资源

---

## 九、关键代码文件清单

根据文档分析，需要实现以下核心文件：

```
Scripts/
├── ArrowLine.cs              # 箭头线核心逻辑（最复杂）
├── LevelManager.cs            # 关卡管理器
├── GridManager.cs             # 网格管理器
├── Dot.cs                     # 节点组件（最简单）
├── LevelAsset.cs              # 关卡资源定义（ScriptableObject）
├── UiManager.cs               # UI管理器（可选，可复用现有）
├── GameMode.cs                # 游戏模式切换（可选）
├── LineAnimation.cs           # 线动画控制（可选）
└── LineColors.cs              # 颜色管理（可选）

DataTable/ 或 ScriptableObject/
└── LevelAsset.cs              # 关卡数据资源
```

---

## 十、总结

这是一个**基于网格的路径规划益智游戏**，核心机制简单但实现精细：

1. **网格系统**提供稳定的坐标基础
2. **节点占用机制**实现精确的碰撞检测
3. **路径移动**通过动态更新节点列表实现
4. **退出机制**提供平滑的胜利反馈
5. **关卡系统**使用ScriptableObject实现数据驱动

**核心玩法**：玩家通过规划点击顺序，避免箭头相互碰撞，在3次失误内完成所有箭头的解谜。

**技术亮点**：
- 基于网格的精确碰撞检测
- 滑动窗口路径管理
- 事件驱动的解耦设计
- 模块化的组件分离
