# 箭头迷宫关卡编辑器使用说明

## 一、功能概述

关卡编辑器支持以下功能：
1. **网格编辑**：创建n*m的基础网格
2. **有效网格选择**：通过鼠标框选标记有效网格（用于限定箭头路径形成特定图案）
3. **箭头填充**：支持自动填充和手动填充两种模式
4. **数据保存**：将关卡数据保存为Json文件

---

## 二、打开编辑器

### 方法1：菜单栏
```
菜单栏 → ArrowMaze → 关卡编辑器
```

### 方法2：代码
```csharp
ArrowMaze.Editor.ArrowLevelEditor.Open();
```

---

## 三、界面说明

### 3.1 工具栏
- **新建**：创建新关卡（会清除当前未保存的数据）
- **加载**：从Json文件加载关卡
- **保存**：保存到当前文件（如果未保存过，会弹出另存为对话框）
- **另存为**：选择保存路径
- **工具切换**：选择网格工具 / 绘制箭头工具

### 3.2 左侧面板

#### 关卡信息
- **关卡名称**：关卡名称
- **难度**：关卡难度等级
- **相机大小**：游戏时相机的OrthographicSize
- **有效网格数**：当前选中的有效网格数量
- **箭头数量**：当前创建的箭头数量

#### 网格设置
- **网格大小**：设置网格的宽度和高度（n*m）
- **网格间距**：每个网格单元之间的间距
- **原点**：网格的原点位置
- **单元格大小**：编辑器中的单元格显示大小（不影响实际游戏）
- **清除所有有效网格**：一键清除所有选中的有效网格

#### 箭头填充配置
- **最小长度**：箭头的最小长度（网格单位，默认2）
- **最大长度**：箭头的最大长度（网格单位，默认10）
- **允许拐弯**：是否允许箭头在路径中拐弯
- **自动填充**：是否使用自动填充模式（仅配置项，不影响手动绘制）

#### 箭头列表
- 显示所有已创建的箭头
- 点击箭头可以选中（高亮显示）
- 点击"×"可以删除箭头

#### 操作按钮
- **自动填充箭头**：在有效网格上自动生成箭头
- **清除所有箭头**：删除所有箭头

### 3.3 右侧网格编辑区

#### 网格显示
- 灰色背景：无效网格
- 绿色半透明：有效网格
- 彩色线条：已创建的箭头路径
- 黄色线条：正在绘制的箭头路径
- 绿色/红色框：选择框（绿色=添加，红色=移除）

---

## 四、操作流程

### 4.1 创建新关卡

1. 点击工具栏的"新建"按钮
2. 设置网格大小（默认20x20）
3. 调整其他设置（可选）

### 4.2 选择有效网格

1. **切换到"选择网格"工具**（工具栏右侧）
2. **框选网格**：
   - 鼠标左键按下并拖拽，框选一个矩形区域
   - 如果起始点在有效网格上：移除选中区域
   - 如果起始点在无效网格上：添加选中区域
3. **重复框选**：可以多次框选，逐步构建图案

**技巧**：
- 可以先框选大区域，再移除不需要的部分
- 支持心型、五角星、像素风图案等任意形状

### 4.3 填充箭头

#### 方法1：手动绘制箭头

1. **切换到"绘制箭头"工具**
2. **绘制路径**：
   - 在有效网格上按下鼠标左键开始绘制
   - 拖拽鼠标，箭头会沿着相邻的有效网格延伸
   - 释放鼠标完成绘制
3. **要求**：
   - 箭头长度必须 >= 最小长度（默认2）
   - 只能在有效网格上绘制
   - 不能与已有箭头重叠

#### 方法2：自动填充箭头

1. **配置填充参数**：
   - 设置最小/最大长度
   - 选择是否允许拐弯
2. **点击"自动填充箭头"按钮**
3. **系统会自动**：
   - 在有效网格上随机生成箭头
   - 满足长度范围要求
   - 避免重叠
   - 根据配置决定是否允许拐弯

**自动填充算法**：
- 随机选择起始点
- 优先沿直线方向延伸
- 如果允许拐弯，会在必要时改变方向
- 达到目标长度或无法继续时停止

### 4.4 保存关卡

1. **点击"保存"或"另存为"**
2. **选择保存路径**（建议保存在Assets目录下）
3. **文件格式**：Json文件（.json）
4. **文件内容**：包含所有网格和箭头数据

---

## 五、数据格式

### 5.1 Json文件结构

```json
{
    "levelName": "Level 1",
    "grid": {
        "width": 20,
        "height": 20,
        "spacing": 1.0,
        "origin": {"x": 0, "y": 0, "z": 0}
    },
    "validGrids": [
        {"x": 5, "y": 5},
        {"x": 6, "y": 5},
        ...
    ],
    "arrows": [
        {
            "serializedPath": [
                {"x": 5, "y": 5},
                {"x": 6, "y": 5},
                {"x": 7, "y": 5}
            ],
            "stepTime": 0.18,
            "lineColor": {"r": 1.0, "g": 0.5, "b": 0.0, "a": 1.0},
            "hitColor": {"r": 1.0, "g": 0.0, "b": 0.0, "a": 1.0},
            "occupyAllNodes": true,
            "startIndex": 0
        },
        ...
    ],
    "fillConfig": {
        "minLength": 2,
        "maxLength": 10,
        "allowTurn": true,
        "autoFill": true
    },
    "cameraSize": 5.0,
    "difficulty": 1,
    "createTime": "2024-01-01 12:00:00",
    "modifyTime": "2024-01-01 12:30:00"
}
```

### 5.2 数据结构说明

- **validGrids**：有效网格列表（HashSet序列化为List）
- **arrows**：箭头列表，每个箭头包含路径、颜色、配置等
- **serializedPath**：箭头路径（Vector2Int序列化为SerializableVector2Int）

---

## 六、使用技巧

### 6.1 创建特定图案

1. **心型图案**：
   - 先框选一个大的矩形区域
   - 再移除不需要的部分，形成心型轮廓
   - 在轮廓内填充箭头

2. **五角星图案**：
   - 使用框选工具逐步构建五角星轮廓
   - 可以先用自动填充，再手动调整

3. **像素风图案**：
   - 参考像素画，框选对应位置的有效网格
   - 可以导入图片作为参考（需要手动对齐）

### 6.2 优化箭头分布

- **手动调整**：自动填充后，可以手动删除或添加箭头
- **长度控制**：调整最小/最大长度，控制箭头长度范围
- **拐弯控制**：关闭"允许拐弯"可以生成更直的箭头

### 6.3 性能优化

- **网格大小**：建议不超过50x50（编辑器性能）
- **箭头数量**：建议不超过100个（游戏性能）
- **有效网格**：尽量紧凑，避免过于分散

---

## 七、常见问题

### Q1: 为什么无法绘制箭头？
- 检查是否切换到"绘制箭头"工具
- 确保在有效网格上绘制
- 确保没有与已有箭头重叠
- 确保箭头长度 >= 最小长度

### Q2: 自动填充没有生成箭头？
- 检查是否有足够的有效网格（至少 >= 最小长度）
- 检查是否有可用网格（未被其他箭头占用）
- 尝试调整最小/最大长度范围

### Q3: 保存的文件在哪里？
- 保存时会显示完整路径
- 建议保存在 `Assets/ArrowMaze/Levels/` 目录下
- 保存后会在Unity中刷新资源数据库

### Q4: 如何加载已保存的关卡？
- 点击"加载"按钮
- 选择之前保存的Json文件
- 编辑器会自动加载并显示

---

## 八、代码结构

### 8.1 数据类
- `ArrowLevelData.cs`：关卡数据结构
- `ArrowLevelDataUtility.cs`：Json序列化工具

### 8.2 编辑器类
- `ArrowLevelEditor.cs`：编辑器窗口主类

### 8.3 命名空间
- `ArrowMaze`：数据类命名空间
- `ArrowMaze.Editor`：编辑器类命名空间

---

## 九、扩展功能（未来）

1. **导入图片**：支持导入图片作为参考，自动识别有效网格
2. **撤销/重做**：支持操作历史记录
3. **网格模板**：预设常用图案模板
4. **批量操作**：批量删除、移动箭头
5. **预览模式**：在编辑器中预览游戏效果

---

## 十、总结

关卡编辑器提供了完整的关卡创建流程：
1. ✅ 创建基础网格
2. ✅ 选择有效网格（形成图案）
3. ✅ 填充箭头（自动/手动）
4. ✅ 保存为Json文件

通过这个编辑器，可以快速创建各种形状的关卡，支持心型、五角星、像素风等任意图案。
